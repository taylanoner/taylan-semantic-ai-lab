name: taylan_semantic_ai_lab
version: "1.0.0"
config-version: 2

profile: taylan_semantic_ai_lab

model-paths: ["models"]
test-paths: ["tests_dbt"]
macro-paths: ["macros"]
target-path: "target"
clean-targets: ["target", "dbt_packages"]

models:
  taylan_semantic_ai_lab:
    staging:
      +materialized: view
    intermediate:
      +materialized: view
    marts:
      +materialized: table
